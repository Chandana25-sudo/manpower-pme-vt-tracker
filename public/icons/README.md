Add real PWA icons here before deploying:

- `icon-192.png` — 192x192
- `icon-512.png` — 512x512

Until these exist, "Add to Home Screen" will still work on most Android/desktop browsers using the favicon, but iOS Safari needs a real `icon-192.png` for a proper home-screen icon.
